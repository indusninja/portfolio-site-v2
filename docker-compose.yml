version: '2'

services:
  jekyll:
    image: bretfisher/jekyll-serve
    restart: always
    volumes:
      - .:/site
    ports:
      - '4000:4000'
      - '35729:35729'
    command: bundle exec jekyll s --force_polling -H 0.0.0.0 -P 4000 --livereload